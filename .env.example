# PostgreSQL (local: docker-compose up -d)
DATABASE_URL="postgresql://a360:a360_dev_password@localhost:5432/news_review?schema=public"

# FastRouter — compliance workflow (server-only, Phase 3+)
FASTROUTER_API_KEY=""

# n8n Meta campaign launch webhook (server-only, Phase 4+)
N8N_WEBHOOK_URL=""

# POST /api/campaign-callback — send as header: x-callback-secret
CALLBACK_SECRET=""

# Vercel Cron + manual cron route protection — send as header: x-cron-secret
CRON_SECRET=""

# Optional: public app URL for docs/callback examples
NEXT_PUBLIC_APP_URL="http://localhost:3000"
